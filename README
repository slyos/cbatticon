A GTK+ battery icon which uses libacpi to be lightweight and fast.

Requires libnotify, libacpi, and GTK+ to build.

Based on code from xbattbar-acpi.

After make, run cbatticon -h for command line options. 
